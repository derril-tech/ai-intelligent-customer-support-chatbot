import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'HeliosCS - Intelligent Customer Support',
  description: 'Financial-grade customer support chatbot for banks, brokerages, and fintechs',
  keywords: ['customer support', 'chatbot', 'financial', 'banking', 'fintech'],
  authors: [{ name: 'HeliosCS Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}
